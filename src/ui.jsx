import React,{useState} from 'react'
import {motion} from 'framer-motion'
import {CalendarDays,Download,FilePlus2,Gauge,MapPinned,Maximize2,Minimize2,MoreHorizontal,Plus,Share2,Users,X} from 'lucide-react'
import {BASE,data} from './lib.js'

export function Topbar({now,update}){
  const start=new Date(`${data.trip.startDate}T05:00:00+05:30`),days=Math.max(0,Math.ceil((start-now)/86400000)),live=now>=new Date(`${data.trip.startDate}T00:00:00+05:30`)&&now<=new Date(`${data.trip.endDate}T23:59:59+05:30`)
  return <header className="topbar"><div className="brand"><img src={`${BASE}icon.svg`} alt=""/><div><span>MUMBAI / 14—17 SEP</span><b>TRIP CONTROL</b></div></div><div className="sync"><i className={update.label.toLowerCase()}/><div><b>{live?'LIVE':`${days}D`}</b><small>{update.label}</small></div></div></header>
}

export function SideNav({active,onChange,onCommand,onInstall}){
  const tabs=[['Now',Gauge],['Plan',CalendarDays],['Map',MapPinned],['Group',Users],['More',MoreHorizontal]]
  return <nav className="side-nav" aria-label="Trip dashboard navigation"><motion.button className="quick" whileTap={{scale:.92}} whileHover={{scale:1.04}} onClick={onCommand} aria-label="Add or change trip"><Plus/><b>Add</b></motion.button><div className="side-nav-sep"/>{tabs.map(([tab,Icon])=><motion.button key={tab} className={active===tab?'active':''} whileTap={{scale:.92}} whileHover={{x:-2}} onClick={()=>onChange(tab)} aria-current={active===tab?'page':undefined}><Icon/><b>{tab}</b></motion.button>)}<div className="side-nav-sep"/><motion.button className="install-nav" whileTap={{scale:.92}} whileHover={{scale:1.03}} onClick={onInstall} aria-label="Install TripOS"><Download/><b>Install</b></motion.button></nav>
}

export function CommandSheet({mode,onClose,notes,setNotes,localExpenses,setLocalExpenses}){
  const [view,setView]=useState(mode==='command'?'menu':mode),[text,setText]=useState(''),[amount,setAmount]=useState(''),[label,setLabel]=useState(''),[participants,setParticipants]=useState(data.members.map(m=>m.id)),[payerId,setPayerId]=useState(data.members[0]?.id||'')
  const saveNote=()=>{if(!text.trim())return;setNotes([...notes,{id:`note-${Date.now()}`,text:text.trim(),createdAt:new Date().toISOString()}]);onClose()}
  const saveExpense=()=>{const value=Number(amount);if(!(value>0)||!label.trim()||!participants.length||!payerId)return;setLocalExpenses([...localExpenses,{id:`local-${Date.now()}`,amount:value,label:label.trim(),participantIds:participants,payerId,category:'local-draft',date:new Date().toISOString().slice(0,10),status:'local-only'}]);onClose()}
  const copy=async value=>{try{await navigator.clipboard.writeText(value)}catch{}onClose()}
  const title=view==='menu'?'Add / change':view==='note'?'Local note':view==='expense'?'Local expense':view==='shared'?'Shared change':view==='installed'?'Installed':'Install TripOS'
  return <div className="sheet-bg" onMouseDown={e=>e.target===e.currentTarget&&onClose()}><section className="sheet"><div className="handle"/><header><div><span>CONTEXTUAL ACTION</span><h2>{title}</h2></div><button onClick={onClose}>×</button></header>{view==='menu'&&<div className="sheet-menu"><button onClick={()=>setView('note')}><b>Note on this phone</b><span>Offline, private to this browser</span></button><button onClick={()=>setView('expense')}><b>Draft an expense</b><span>Local ledger until committed</span></button><button onClick={()=>setView('shared')}><b>Change the shared trip</b><span>Copy a precise ChatGPT command</span></button></div>}{view==='note'&&<div className="form"><textarea autoFocus placeholder="Bag is at reception, call before leaving…" value={text} onChange={e=>setText(e.target.value)}/><button className="save" onClick={saveNote}>Save offline note</button></div>}{view==='expense'&&<div className="form"><input autoFocus inputMode="decimal" placeholder="Amount ₹" value={amount} onChange={e=>setAmount(e.target.value)}/><input placeholder="What was it?" value={label} onChange={e=>setLabel(e.target.value)}/><div className="selector-grid"><div className="selector-block"><label>Paid by</label><div className="payer-select">{data.members.map(m=><button type="button" key={m.id} className={payerId===m.id?'active':''} onClick={()=>setPayerId(m.id)} title={m.name}>{m.initials}</button>)}</div></div><div className="selector-block"><label>Split between</label><div className="participant-select">{data.members.map(m=><button type="button" key={m.id} className={participants.includes(m.id)?'active':''} onClick={()=>setParticipants(participants.includes(m.id)?participants.filter(id=>id!==m.id):[...participants,m.id])}>{m.initials}</button>)}</div></div></div><small>Local-only. Shared expenses must be committed through ChatGPT.</small><button className="save" onClick={saveExpense}>Add local draft</button></div>}{view==='shared'&&<div className="sheet-menu"><button onClick={()=>copy('Add this to the Mumbai TripOS inbox, research it, choose the best slot, validate, commit and deploy: ')}><b>Add a place</b><span>Candidate ingestion</span></button><button onClick={()=>copy('Replan Mumbai TripOS around this change, preserve fixed bookings, update travel legs, validate, commit and deploy: ')}><b>Replan</b><span>Safe downstream change</span></button><button onClick={()=>copy('Add this expense to the Mumbai TripOS shared ledger. Record the payerId for the person who actually paid, the participantIds for everyone sharing it, amount, date and category. If I have not said who paid, ask me for the payer before committing. Validate, commit to main and deploy: ')}><b>Share an expense</b><span>Canonical ledger with payer</span></button></div>}{view==='install'&&<div className="install-help"><div className="install-step"><i>01</i><div><b>Chrome / Android</b><span>Open the browser menu and choose Install app or Add to Home screen. If the browser exposes the native install prompt, the Install button launches it directly.</span></div></div><div className="install-step"><i>02</i><div><b>Safari / iPhone</b><span>Use Share → Add to Home Screen. The installed dashboard keeps the app shell and offline vault cached.</span></div></div></div>}{view==='installed'&&<div className="install-help"><div className="install-step"><i>✓</i><div><b>TripOS is already running as an installed app.</b><span>Updates are detected through version.json on resume, reconnect and the regular update check.</span></div></div></div>}</section></div>
}

async function shareResource(resource){
  try{const r=await fetch(resource.path),blob=await r.blob(),extension=resource.type==='pdf'?'pdf':'svg',file=new File([blob],`${resource.id}.${extension}`,{type:blob.type||(resource.type==='pdf'?'application/pdf':'image/svg+xml')});if(navigator.canShare?.({files:[file]}))return navigator.share({title:resource.label,files:[file]});if(navigator.share)return navigator.share({title:resource.label,url:new URL(resource.path,location.origin).href})}catch{}
}

export function ResourceViewer({resource,onClose}){
  const [boarding,setBoarding]=useState(Boolean(resource.boarding))
  const source=resource.type==='pdf'?`${resource.path}#toolbar=0&navpanes=0&scrollbar=0&view=Fit` : resource.path
  return <div className={`viewer${boarding?' boarding':''}`}><header><div><span>{boarding?'BOARDING MODE':'OFFLINE VAULT'}</span><b>{resource.label}</b><small>{resource.meta}</small></div><div className="viewer-actions"><button onClick={()=>setBoarding(v=>!v)}>{boarding?<><Minimize2 size={14}/> Exit</>:<><Maximize2 size={14}/> Boarding</>}</button>{!boarding&&<button onClick={()=>shareResource(resource)}><Share2 size={14}/> Share</button>}<button onClick={onClose}><X size={14}/> Close</button></div></header><div className="viewer-body">{resource.type==='pdf'?<object data={source} type="application/pdf" aria-label={resource.label}><iframe src={source} title={resource.label}/></object>:<img src={resource.path} alt={resource.label}/>}</div>{!boarding&&<footer>Cached after a successful online load. This Pages site is public; these copies contain booking information.</footer>}</div>
}
